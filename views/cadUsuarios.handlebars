
  
  
  <form id="formUsers" action="/usuarios/cadUser" method="POST" onsubmit="return valida()">
    <fieldset>
      <legend>Adicionar Usuário</legend>
    <label>Nome:</label><input type="text" class="titulo" id="titulo" name="nome" placeholder="Nome do Usuário" required><br><br>
    
    <label>E-mail:</label>
    <input type="email" class="subti" id="email" name="email" placeholder="Seu e-mail aqui" required><br><br>
    
    <label>Login:</label>
    <input type="text" class="subti" id="login" name="login" placeholder="Escreva nickname aqui" required></textarea><br><br>
    
    <label>Senha:</label>
    <input type="password" class="subti" id="senha" name="senha" placeholder="Digite sua senha" required><br><br>
    
    <label>Senha:</label>
    <input type="password" class="subti" id="senha2" name="senha2" placeholder="Repita sua senha" required><br><br>
    <label>Usuário Ativo:</label>
    <select class="ver" id="ver" name="ver">
      <option value="1">Sim</option>
      <option value="0">Não</option>
    </select><br><br>
    
    <input type="submit" value="Salva Usuário"><br><br>
    </fieldset>
  </form>











    <a href="/usuarios/admUsuarios">Voltar</a>
    <script>
        function valida(){
            d=document.formUsers;
            if(d.nome.value==""){
                alert("O campo "+d.nome.name+" deve ser preenchido!");
                d.nome.focus();
                return false;
            }
            if(d.login.value==""){
                alert("O campo "+d.login.name+" deve ser preenchido!");
                d.nome.focus();
                return false;
            }

            if(d.email.value==""){
                alert("O campo "+d.email.name+" deve ser preenchido!");
                d.nome.focus();
                return false;
            }

            if(d.senha.value != d.senha2.value){
                    alert("OS CAMPOS DE SENHA DEVEM SER IGUAIS!");
                    d.senha.focus();
                    return false;
                }
        }           
    </script>